set(COMPONENT_SRCDIRS src 
                      src/devices)

set(COMPONENT_ADD_INCLUDEDIRS src)

set(COMPONENT_PRIV_INCLUDEDIRS src/devices)

set(COMPONENT_PRIV_REQUIRES arduino)

register_component()
